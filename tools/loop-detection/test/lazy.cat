lazy

let good = r ; s
let bad = missing
with co from missing_generator(good)
let ca = fr | co
with TLBI-after from missing_generator(good)
let unused_tlbi = TLBI-after ; missing
let demanded_tlbi = TLBI-after ; r
